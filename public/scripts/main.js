
function mainmethos() {
    var seatsArray = [];

    localStorage.setItem("tableArray", JSON.stringify(seatsArray));

    // var timeArray = [];

    // localStorage.setItem("timeArray", JSON.stringify(timeArray));
}
mainmethos();